sudo: false
language: ruby
rvm:
  - 2.4.0
cache:
  - bundler
  - cargo
git:
  depth: 10
  submodules: false

before_install:
  - sed -i 's/git@github.com:/https:\/\/github.com\//' .gitmodules
  - git submodule update --init --recursive

script:
  - script/cibuild
